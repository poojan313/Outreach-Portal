version: '3.3'

services:
  #service 1: definition of mysql database
  db:
    image: mysql:latest
    container_name: mysql-db2
    environment:
      - MYSQL_ROOT_PASSWORD=poojankhatri
      - MYSQL_USER=root
    ports:
      - "3306:3306"
    restart: always
